# Mani 项目标准工作流

> 当前项目处于半结项维护状态。以下“新任务”流程仅作为历史规范保留；未经项目
> 所有者明确重新开放，不启动新任务。当前工作限于已有四项任务的调整、复验和归档。

## 1. 启动与接管

每次新会话先读取 `AGENTS.md`、`PROJECT_STATE.yaml` 和两个任务目录的
`README.md`。先确认本地与服务器的真实文件状态，不依赖聊天记忆推断。

服务器连接：

```bash
ssh -p 39232 root@115.190.90.101
```

项目目录：

```text
/vepfs-mlp2/queue013/public/huangborui/mani-project
```

## 服务器写入红线

服务器上唯一允许创建、修改、移动、重命名或删除内容的范围是：

```text
/vepfs-mlp2/queue013/public/huangborui
```

只有该目录及其子目录可以写入。服务器上其他所有路径一律视为只读，即使当前
账号具有权限也不得改动。执行任何服务器写操作前，必须先解析并核对目标路径
位于上述根目录内；路径不明确、包含未解析变量或可能越界时立即停止。读取其他
路径用于确认系统、GPU 或依赖状态可以，但禁止在那里创建缓存、环境、日志、
软链接、临时文件或任何其他内容。

默认使用物理 GPU 1，因为 GPU 0 可能被其他任务占用：

```bash
CUDA_VISIBLE_DEVICES=1 <command>
```

项目允许在确有收益时使用两张 A100。启用双卡前必须：

1. 用只读检查确认 GPU 0 和 GPU 1 均空闲，并确认不会抢占他人进程；
2. 确认训练或评估实现真正支持多 GPU，例如 DDP、明确的数据/环境切分，或
   两个相互独立的 checkpoint 评估进程；
3. 记录物理卡到进程的映射、启动拓扑、每卡显存和输出归属；
4. 为并发进程使用独立日志和输出，避免 checkpoint 或结果文件竞争写入。

仅设置 `CUDA_VISIBLE_DEVICES=0,1` 不代表单卡程序会自动双卡加速。若 GPU 0
已有进程或多卡方案未经验证，则继续只用 GPU 1。

## 2. 新任务调查

1. 确认 ManiSkill 版本、原生环境 ID、观测空间、动作空间、控制模式、episode
   horizon 和严格 success predicate。
2. 调查同版本官方 baseline 的脚本、commit 和默认超参数。
3. 用随机策略或未训练策略建立失败基线。
4. 检查已有 trainer 是否真的兼容该任务，尤其是 success 字段、partial reset、
   pose shape、action mask 和 checkpoint 格式。
5. 在 `tasks/<new_task>/README.md` 建立任务入口，在 `docs/` 新建实验日志。

## 3. 实验与训练

1. 每轮使用唯一的实验编号、配置文件、run 目录和日志文件。
2. 从旧 checkpoint 继续训练时，先复制源权重到新 run 作为不可变来源记录。
3. 启动前记录完整命令、Python 环境、git/source commit、GPU、随机种子和 PID。
4. 启动后只做一次快速健康检查；稳定后每三分钟检查一次。
5. 每次监控记录进度、GPU、在线评估、checkpoint 数量和异常。
6. 不因为在线小样本峰值停止或晋升模型。

## 4. 模型筛选

1. 保留并评估全部周期 checkpoint 和 final checkpoint。
2. 第一轮使用固定 100 seeds，同环境、同 horizon、确定性 actor 全筛选。
3. 对 top candidates 使用另一组独立 100 seeds 复验。
4. 根据两组一致性选候选；不能只选最后 checkpoint。
5. 对新候选和旧 best 使用完全相同的 1,000 环境、相同向量 seed 正式对比。
6. 记录严格成功、抓取、精确到位、释放、静止、XY/Z 误差和失败分类。
7. 只有确认提升后才复制为新 best；原 checkpoint 永久保留。
8. 为晋升副本计算 SHA-256，本地与服务器哈希必须一致。

## 5. 视频验证

1. 使用确定性 actor 和真实最佳权重录制。
2. 视频必须经过 `ffprobe` 验证 codec、分辨率、帧数和时长，不能只检查文件存在。
3. 生成独立成功轨迹，并可额外生成并行环境总览。
4. SAPIEN 的系统 `libvulkan` 警告可在内置 Vulkan 回退成功时接受，但必须以
   实际有效 MP4 为准。
5. 视频放入独立、带模型阶段标识的目录，禁止覆盖旧视频。

## 6. 报告与交接

每轮至少更新：

- 实验参数和运行时间；
- 成功与失败的实验，包括失败原因；
- checkpoint 筛选表；
- matched-seed 正式评估；
- 最佳模型路径和 SHA-256；
- 视频路径；
- 当前瓶颈与下一步建议。

收尾时同步本地与服务器，备份将被修改的服务器报告，确认无训练/评估进程
残留，并更新 `PROJECT_STATE.yaml`。下次会话以该文件为事实入口。
